package com.event.wear.platform.transactions.domain.model.queries;

import com.event.wear.platform.transactions.domain.model.valueobjects.ProductId;

public record GetProductItemByIdQuery(ProductId productId) {
}
